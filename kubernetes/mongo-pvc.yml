apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc
  namespace: employee-management-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-auto
  resources:
    requests:
      storage: 5Gi