apiVersion: v1
kind: Service
metadata:
  name: employee-app-service
  namespace: employee-management-app
spec:
  selector:
    app: employee-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30950
  type: NodePort